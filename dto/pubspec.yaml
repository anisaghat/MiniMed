name: dto
description: Modèles de données (DTO) pour MiniMed
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  freezed_annotation: ^2.4.4
  cloud_firestore_odm: 1.0.0-dev.88
  cloud_firestore: ^5.0.0
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.13
  freezed: ^2.5.7
  json_serializable: ^6.8.0